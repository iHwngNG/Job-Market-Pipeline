name: 'job_market_pipeline'
version: '1.0.0'

profile: 'job_market'

model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]

models:
  job_market_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: view
      +schema: marts
